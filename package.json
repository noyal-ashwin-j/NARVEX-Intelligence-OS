{
  "name": "narc-intel-platform",
  "version": "1.0.0",
  "description": "Statewide Narcotic Intelligence & Preventive Risk Monitoring Platform (NARC-INTEL / N-RISE)",
  "private": true,
  "scripts": {
    "dev": "npm --prefix client run dev",
    "server": "npm --prefix server start",
    "server:dev": "npm --prefix server run dev",
    "test": "npm --prefix server test",
    "test:security": "npm --prefix server run test:security",
    "test:authorization": "npm --prefix server run test:authorization",
    "test:audit-chain": "npm --prefix server run test:audit-chain",
    "test:agent-security": "npm --prefix server run test:agent-security",
    "seed": "npm --prefix server run seed",
    "build": "npm --prefix client run build"
  }
}
